# Copy to .env.local (recommended) or .env — never put real keys in this file or commit them.

# Google Gemini (used first if set) — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional model override (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# OpenAI (used only if Gemini is not configured)
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
